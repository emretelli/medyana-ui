import { PatientFilterPipe } from './patient-filter.pipe';

describe('PatientFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new PatientFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
